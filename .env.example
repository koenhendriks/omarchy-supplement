# Copy to .env and fill in the real values.
# Consumed by install-vpn.sh, install-omarchy-jira-plugin.sh and
# install-jira-cli.sh.

# MassMarket (OpenVPN, imported into openvpn3)
VPN_MASSMARKET_USERNAME=
VPN_MASSMARKET_PASSWORD=

# Sandwave (IKEv2/IPsec via strongSwan, NOT OpenVPN -- see vpn/README)
# EAP identity and password; password-flags=0 means it was stored, not prompted.
VPN_SANDWAVE_USERNAME=
VPN_SANDWAVE_PASSWORD=

# Jira (Atlassian API token, from id.atlassian.com > Security > API tokens).
# Optional: without it the menu Jira plugin still opens tickets, it just cannot
# look up which projects exist or what a ticket is called. jira-cli needs it for
# every command, so install-jira-cli.sh cannot configure it without one.
JIRA_API_TOKEN=
